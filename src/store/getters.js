const getters = {
    direction: state => state.app.direction,
    userInfo:state=>state.app.userInfo,
    predictInfo:state=>state.app.predictInfo,
    rankInfo:state=>state.app.rankInfo,
    chainInfo:state=>state.app.chainInfo,
    awardInfo:state=>state.app.awardInfo,
}
export default getters
